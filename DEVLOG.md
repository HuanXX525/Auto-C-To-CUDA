# DEVLOG

## [2026-04-11]

### Added

- 在test加了内联声明复制测试代码

### Fixed

- 修复了CUDA线程ID与1-based loop canonical form不一致的问题

## [2026-04-09]

### Added

- 加了测试脚本和测试文件夹验证程序的正确性

### Changed

- 更改了程序的输出逻辑，按照原相对路径输出

## [2026-04-02]

### Added

- 纯函数白名单过滤和纯函数检测
- 内联块内变量重命名

## [2026-04-01]

### Documented

- 函数内联会出现的情况：使用静态变量不能内联、调用其他函数、使用外部变量

### Added

- 函数内静态变量和静态函数的检测

## [2026-03-31]

### Added

- 根据CUDA Math Api v13.2中的`Single Precision Mathematical Functions`,`Double Precision Mathematical Functions`,`Integer Mathematical Functions`与CPP Reference中的`Common mathematical functions`查找了同名函数，更新了safe_functions的内容

### Documented

- [ ] 问题：跨文件内联会导致目的地文件缺失被内联函数的类型定义、宏及头文件依赖，引发编译错误
  <!-- - 拿到project后宏就已经展开了，因此需要注意的仅有特殊类型的声明，以及函数的声明，外部变量的声明，尝试原文件提取声明与目标文件冲突检测 -->

循环中可能存在的函数调用类型

- 无定义的函数调用
  - 无定义的math库函数（CUDA设备端存在相同的定义）：手动标记为safe_functions
  - 无定义的其他库函数：放弃并行
- 有定义的函数
  - 手动标记的纯函数：设备化
  <!-- - 尝试确定为纯函数（相同的输入，永远会得到相同的输出，值传递，无全局变量和静态变量的使用）：设备化 -->
  - 非纯函数：内联分析
  - 存在递归的函数：放弃并行

## [2026-03-23]

### Changed

- 因为内联的需求将全流程分两遍处理

### Added

- 借助ROSE调用图获取函数的拓扑排序，从而获取内联顺序，实际上分两遍处理无需排序亦可，这里保留排序待用

### Documented

关于内联，目的是让包含调用的循环能够参与到依赖测试，是否所有的循环都需要内联？

- 对于函数内没有循环的函数内联一定能提高性能
- 对于函数内存在循环的函数内联不一定能提高性能
- 当对所有循环内能够内联的函数执行内联后，可能会导致依赖测试不通过
- 若选择部分循环不并行化而是标记安全并放弃对当前循环的并行，调用并行化后的函数，性能可能会比内联更高
- 若内联：既然选择内联，那么就不能将并行化后的代码内联进来，因此要注意内敛顺序

## [2026-03-19]

### Fixed

- 修复了多文件时直接遍历全局语法树的问题

### Documented

- [ ] ***BUG***：经测试BUG来自原项目，测试文件 `tests/multifiles/bug/bug_test_ori.c`
- [ ] ***TODO***：循环并行顺序，内联展开重名问题（对原项目分析存在影响）

## [2026-03-17]

### Fixed

- 在`Translate.cpp`处理完函数体后未恢复最外层函数收集容器初始化状态的BUG

### Added

- 添加了部分日志输出

## [2026-03-15]

### Added

- 在预处理阶段标记白名单函数以及内联，先标记函数属性检查是否递归是否安全是否存在定义然后内联

### Documented

- [x] ***BUG***：经测试BUG来自原项目，当前程序不论是源程序还是改过的程序在处理 `tests/inline/main_bug.c` 均出现崩溃（已解决，问题出自外层循环收集表未初始化）
- 测试内联没问题，待处理完第一条BUG后继续

## [2026-03-14]

### Documented

- 即使不启用 `-rose:ase:merge` 也能找到函数定义
- 存在定义的函数尝试内联展开来进行依赖分析（问题）
  
  - 被调用函数先于循环被优化，会导致ROSE无法分析内联后的代码

    - [x] 在预处理阶段标记白名单函数以及内联，先标记函数属性检查是否递归是否安全是否存在定义然后内联

  - [x] 存在无法找到定义的数学库函数，如数学库函数，使用自定义函数白名单，填入白名单的函数**必须不存在副作用**例如"sin, cos"

### Added

- `config.json`，`safe_math_functions`作为可并行函数白名单
- 过滤白名单的安全函数

## [2026-03-13]

### Added

- 添加了 `tools` 用于测试ROSE

### Documented

- 先尝试解决跨文件的问题，使用 `-rose:ast:merge` 查看效果，测试成功测试结果如下

  ```text
  ========== 跨文件函数分析报告 ==========

    发现调用: loop (位于: /home/huanxx/ShareWithWindows/Auto-C-To-CUDA/tests/multifiles/main.c)
    [★] 状态: 跨文件链接成功！
        -> 定义文件: /home/huanxx/ShareWithWindows/Auto-C-To-CUDA/tests/multifiles/head.c
        -> 定义行号: 5
    ------------------------------------------
  ```

## [2026-03-11]

### Documented

- [x]  由于本项目未考虑多文件的输入，当使用分步编译时不同文件中生成的kernel会重名导致链接阶段失败，可以通过在kernel中添加文件名加随机数后缀的方式来解决
- [ ]  当前全局变量的处理策略是在每次使用时生成副本，使用完后再复制回去，显然无法处理异步的问题
- [x]  不能使用分步编译，如果循环中涉及函数调用，则必须对所调用的函数进行转化，因此必须支持跨文件查看函数定义的能力；鉴于此第一条的随机后缀就可以改成文件名加全局计数的方式

## [2026-03-8]

### Changed

- 在主文件中添加了源文件判别
- 在主文件中添加了后缀转换

## [2025-11-19]

### Changed

- 修改了Makefile对libtool的使用，使项目能正确编译
- 重写了Makefiles，将编译结果输出到了build目录下

### Documented

- 添加了DEVLOG.md文件，记录开发日志
